name: Ganpati

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Enable RDP & Add User
        run: |
          $user = "Ganpatipc"
          $pass = "GE102030405060807090MN"
          $secure = ConvertTo-SecureString $pass -AsPlainText -Force

          if (-not (Get-LocalUser -Name $user -ErrorAction SilentlyContinue)) {
              New-LocalUser -Name $user -Password $secure -AccountNeverExpires
              Add-LocalGroupMember -Group "Administrators" -Member $user
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member $user
          }

          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Restart-Service -Name TermService

      - name: Install Tailscale
        run: |
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile "$env:TEMP\tailscale.msi"
          Start-Process msiexec.exe -ArgumentList "/i", "`"$env:TEMP\tailscale.msi`"", "/quiet", "/norestart" -Wait

      - name: Connect to Tailscale
        run: |
          $AuthKey = "${{ secrets.TAILSCALE_AUTH_KEY }}"
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=$AuthKey --hostname=GTE-RDP
          Start-Sleep -Seconds 10
          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "RDP_IP=$ip" >> $env:GITHUB_ENV

      - name: Show Login Info
        run: |
          Write-Host "==================================="
          Write-Host "✅ Tailscale RDP is ready!"
          Write-Host "IP Address : $env:RDP_IP"
          Write-Host "Username   : Ganpatipc"
          Write-Host "Password   : GE102030405060807090MN"
          Write-Host "==================================="
          while ($true) { Start-Sleep -Seconds 300 }
